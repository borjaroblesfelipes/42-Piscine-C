/*
int	main(void)
{
	char			str[22];
	char			to_find[22];
	unsigned int	x;

	x = 0;
	printf("Introduce la cadena donde buscar:  \n");
	scanf("%s", str);
	printf("Introduce la cadena a encontrar:  \n");
	scanf("%s", to_find);
	if (ft_strstr(str, to_find) == 0)
	{
		printf("La cadena no fue encontrada.\n");
	}
	else
	{
		printf("La cadena fue encontrada en la posición:  %s.\n", ft_strstr(str, to_find));
	}
}
*/
